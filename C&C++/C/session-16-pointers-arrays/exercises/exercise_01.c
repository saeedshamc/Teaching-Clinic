/*
 * تمرین ۱ — مجموع عناصر با اشاره‌گر
 * TODO: با اشاره‌گر (نه ایندکس) مجموع را حساب کنید.
 */

#include <stdio.h>

int main(void) {
    int arr[5] = {10, 20, 30, 40, 50};
    int sum = 0;

    /* TODO: با حلقه و اشاره‌گر مجموع را محاسبه کنید */
    for (int *q = arr; q < arr + 5; q++) {
        sum += *q;
    }

    printf("مجموع = %d\n", sum);   /* باید 150 باشد */
    return 0;
}
