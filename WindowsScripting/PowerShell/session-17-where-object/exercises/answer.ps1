# پاسخ تمرین - جلسه 17: Where-Object برای فیلتر کردن داده
# این یکی از راه‌حل‌های ممکن است؛ راه‌حل‌های دیگر هم می‌توانند درست باشند.

$result = 1..100 | Where-Object { $_ % 3 -eq 0 -and $_ % 5 -eq 0 }
Write-Host "اعداد بخش‌پذیر بر 3 و 5: $result"
