# جلسه 50: پروژه نهایی: وب‌سایت چندصفحه‌ای — ۱.۵ ساعت

## 🎯 اهداف یادگیری

1. چهار صفحه linked: home، about، services، contact
2. CSS modular: reset + styles
3. navbar active state و responsive
4. ترکیب تمام مفاهیم دوره
5. deploy-ready structure

---

## ⏱️ برنامه زمانی (۹۰ دقیقه)

| زمان | موضوع |
|------|--------|
| 0–15 | برنامه پروژه و wireframe |
| 15–40 | صفحات و navigation |
| 40–60 | CSS و responsive |
| 60–80 | a11y و polish |
| 80–90 | ارائه و بازخورد |

---

## ۱. ساختار

```
examples/
  index.html
  about.html
  services.html
  contact.html
  css/reset.css
  css/styles.css
```

---

## ۲. Navigation

لینک نسبی بین صفحات — `.nav__link--active`

---

## ۳. Responsive

Grid auto-fit، clamp typography، mobile nav

---

## ۴. a11y

semantic، aria-label nav، label for inputs

---

## ۵. Checklist نهایی

✅ 4 pages ✅ reset ✅ responsive ✅ forms ✅ footer

---

## ۶. گسترش

اضافه کردن JS، backend، deploy static

---

## 📁 فایل‌های این جلسه

| فایل | توضیح |
|------|--------|
| [index.html](./examples/index.html) | صفحه خانه |
| [about.html](./examples/about.html) | درباره |
| [services.html](./examples/services.html) | خدمات |
| [contact.html](./examples/contact.html) | تماس |
| [css/reset.css](./examples/css/reset.css) | reset |
| [css/styles.css](./examples/css/styles.css) | استایل اصلی |
| [01_hero_pattern.html](./examples/01_hero_pattern.html) | الگوی hero |
| [02_nav_pattern.html](./examples/02_nav_pattern.html) | الگوی nav |
| [03_form_pattern.html](./examples/03_form_pattern.html) | الگوی فرم |
| [04_project_overview.html](./examples/04_project_overview.html) | نمای کلی |

---

## ✅ چک‌لیست پایان جلسه

- [ ] همه مثال‌ها در مرورگر باز و بررسی شدند
- [ ] DevTools (F12) برای بررسی عناصر استفاده شد
- [ ] تمرین‌های کلاسی انجام شد
- [ ] تکلیف خانه در `exercises/question.md` شروع شد
- [ ] مفاهیم کلیدی در دفترچه یادداشت خلاصه شد

---

## 📚 مطالعه تکمیلی

- [MDN Web Docs](https://developer.mozilla.org/fa/)
- [web.dev](https://web.dev/)

---

**جلسه قبل:** [۴۹ — cross-browser](../session-49-cross-browser/) | **بعد:** —


---

---

## راهنمای یادگیری خودآموز

### عنوان، شماره و زمان

**جلسهٔ 50 — پروژهٔ نهایی چندبخشی | ۹۰ دقیقه**

### پیش‌نیازها

- مفاهیم جلسهٔ 49: سازگاری مرورگرها
- توانایی باز کردن فایل HTML در مرورگر و استفادهٔ پایه از DevTools
- اجرای مثال‌های جلسه پیش از شروع تمرین

### اهداف قابل‌اندازه‌گیری

در پایان جلسه باید بتوانید:

1. مفهوم «پروژهٔ نهایی چندبخشی» را با یک مثال و واژگان فارسی/English توضیح دهید.
2. نمونهٔ موجود را بدون کپی کورکورانه بازسازی و نتیجه را در مرورگر بررسی کنید.
3. حداقل سه خطای رایج این موضوع را با DevTools تشخیص دهید.
4. راه‌حل خود را از نظر معنا، خوانایی، responsive بودن و accessibility ارزیابی کنید.

### چرا این جلسه مهم است؟

موضوع **پروژهٔ نهایی چندبخشی** یک جزیرهٔ جدا نیست؛ مستقیماً روی کیفیت، نگهداری و تحویل رابط در چرخهٔ واقعی توسعه اثر می‌گذارد. یادگیری آن به‌صورت تصمیم‌محور باعث می‌شود به‌جای حفظ کردن تگ یا property، بتوانید برای محتوای واقعی راه‌حل درست انتخاب کنید و دلیل انتخابتان را توضیح دهید.

### مفهوم عمیق: از کد تا نتیجه

مرورگر کد را از بالا به پایین parse می‌کند، آن را به ساختارهای داخلی مانند DOM و CSSOM تبدیل می‌کند و سپس style، layout و paint را انجام می‌دهد. هنگام کار با **پروژهٔ نهایی چندبخشی** همیشه سه پرسش داشته باشید: «معنای این کد چیست؟»، «مرورگر چگونه آن را محاسبه می‌کند؟» و «کاربر با keyboard، موبایل یا صفحه‌خوان چه تجربه‌ای دارد؟». این مدل ذهنی از آزمون‌وخطای تصادفی جلوگیری می‌کند.

### مسیر چندبخشی پروژهٔ نهایی: Responsive Landing

1. **تعریف:** مخاطب، پیام اصلی، محتوای واقعی و wireframe موبایل را مشخص کنید.
2. **ساختار:** `header/nav/main/section/footer`، heading منطقی، تصاویر دارای alt و فرم دارای label بسازید.
3. **سیستم بصری:** tokenهای رنگ/فاصله/تایپ، BEM و componentهای nav، hero، card و form را پیاده کنید.
4. **چیدمان:** پایهٔ mobile-first، Flexbox برای componentها و Grid برای بخش‌های دوبعدی؛ breakpoint فقط هنگام شکستن محتوا.
5. **تعامل:** focus-visible، hover، transition کوتاه و احترام به `prefers-reduced-motion`.
6. **کنترل کیفیت:** keyboard، کنتراست، validation، ۲۰۰٪ zoom، دو مرورگر و عرض‌های 320/768/1280 پیکسل.
7. **تحویل:** ساختار فایل خوانا، README کوتاه و گزارش سه تصمیم طراحی؛ پروژه در همین پوشهٔ تمرین می‌ماند.

**معیار پذیرش:** بدون overflow افقی، بدون لینک/تصویر شکسته، یک `h1`، فرم قابل‌استفاده با keyboard و امتیاز قابل‌قبول Accessibility در Lighthouse.


### سه اشتباه رایج و اصلاح آن‌ها

1. **کپی‌کردن بدون پیش‌بینی نتیجه:** پیش از refresh بنویسید انتظار دارید چه تغییری ببینید؛ سپس با DevTools اختلاف را پیدا کنید.
2. **حل ظاهر و نادیده‌گرفتن معنا:** ابتدا ساختار، ترتیب محتوا و دسترسی با keyboard را درست کنید؛ بعد سراغ polish بصری بروید.
3. **تغییرهای بزرگ و تست دیرهنگام:** هر بار یک تغییر کوچک انجام دهید و در عرض باریک و عریض بررسی کنید.
4. **استفاده از workaround فوری:** پیش از `!important`، selector پیچیده یا مقدار جادویی، علت اصلی را در Computed/Box Model بیابید.

### مثال گام‌به‌گام

فایل [`examples/01_hero_pattern.html`](./examples/01_hero_pattern.html) را مبنا قرار دهید:

1. فایل را در مرورگر باز کنید و بدون دیدن کد، اجزای صفحه را نام ببرید.
2. سورس را بخوانید و هر بخش مرتبط با «پروژهٔ نهایی چندبخشی» را با یک کامنت مشخص کنید.
3. یک مقدار یا عنصر را تغییر دهید و نتیجهٔ مورد انتظار را قبل از refresh یادداشت کنید.
4. با Inspect، مقدار نهایی و دلیل اعمال‌شدن آن را بررسی کنید.
5. تغییر را با keyboard و عرض ۳۲۰ پیکسل آزمایش کنید.
6. کد آزمایشی را به نسخه‌ای خوانا با نام‌گذاری معنادار refactor کنید.

### الگوی بهتر (Better Pattern)

چرخهٔ **content → structure → style → test → refactor** را رعایت کنید: با محتوای واقعی شروع کنید، کمترین ساختار معنایی لازم را بسازید، style را از ساده به پیچیده اضافه کنید، در هر مرحله تست کنید و فقط پس از کارکرد صحیح abstraction بسازید. این الگو از over-engineering و CSS شکننده جلوگیری می‌کند.

### تمرین‌ها

#### تمرین آسان

مثال اصلی را از حافظه در یک فایل تازه بازسازی کنید و سه تصمیم خود دربارهٔ «پروژهٔ نهایی چندبخشی» را در کامنت‌ها توضیح دهید. سپس یک screenshot یا یادداشت از بررسی DevTools ثبت کنید.

#### تمرین چالشی

مثال را با محتوای فارسی واقعی گسترش دهید؛ نسخه باید در عرض ۳۲۰ تا ۱۲۸۰ پیکسل، با Tab، و بدون خطای مسیر فایل کار کند. یک محدودیت عمدی ایجاد کنید، آن را تشخیص دهید و علت/اصلاح را بنویسید.

### راهنمای حل

- ابتدا معیار پذیرش را بنویسید: محتوای کامل، ساختار معتبر، نبود overflow و focus قابل‌مشاهده.
- نسخهٔ آسان را با کوچک‌ترین کد ممکن بسازید و با نمونه مقایسه کنید.
- در تمرین چالشی از Responsive Mode و پنل Computed استفاده کنید؛ فقط پس از یافتن علت، کد را تغییر دهید.
- پاسخ یکتا نیست؛ راه‌حل خوب باید **قابل توضیح، قابل تست و قابل نگهداری** باشد.

### خودآزمایی

1. موضوع اصلی این جلسه در کدام مرحله از کار مرورگر یا تجربهٔ کاربر اثر می‌گذارد؟
2. چگونه بدون حدس‌زدن می‌فهمید کد شما واقعاً اعمال شده است؟
3. یک تصمیم ضعیف در این موضوع چه اثری بر موبایل یا accessibility دارد؟
4. چه زمانی راه‌حل ساده‌تر از abstraction پیچیده بهتر است؟

<details>
<summary>راهنمای پاسخ</summary>

پاسخ باید به‌ترتیب به مدل DOM/CSSOM و render، مشاهده در DevTools و آزمون واقعی، اثر بر ترتیب/خوانایی/focus، و اصل «کمترین پیچیدگی لازم» اشاره کند. پاسخ را با یک مثال از فایل همین جلسه کامل کنید.
</details>

### جمع‌بندی و پل جلسهٔ بعد

اکنون باید بتوانید **پروژهٔ نهایی چندبخشی** را توضیح دهید، در نمونه تشخیص دهید، به‌صورت مرحله‌ای پیاده کنید و با ابزار مرورگر بسنجید. گام بعدی **مسیر مستقل ساخت پروژه و تمرین مستمر** است؛ پیش از رفتن، تمرین چالشی را نگه دارید تا در جلسهٔ بعد روی همان خروجی توسعه دهید.

