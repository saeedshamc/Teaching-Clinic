# جلسه ۵: لیست‌ها — ul، ol و dl (۱.۵ ساعت)

## 🎯 اهداف یادگیری

پس از این جلسه می‌توانید:
- لیست نامرتب `<ul>` و مرتب `<ol>` بسازید
- با `<li>` آیتم لیست بنویسید
- لیست تو در تو (nested) ایجاد کنید
- لیست تعریفی `<dl>`, `<dt>`, `<dd>` را به کار ببرید
- attributeهای `type`, `start`, `reversed` را بشناسید

---

## ⏱️ برنامه زمانی (۹۰ دقیقه)

| زمان | موضوع |
|------|--------|
| 0–10 | کاربرد لیست در وب |
| 10–30 | ul و li |
| 30–45 | ol، start، reversed |
| 45–60 | nested lists |
| 60–75 | dl، dt، dd |
| 75–85 | تمرین کلاسی |
| 85–90 | تکلیف، Q&A |

---

## ۱. چرا لیست؟

لیست‌ها محتوای **گروه‌بندی‌شده** را ساختاریافته نشان می‌دهند: منو، مراحل، مواد، واژه‌نامه.

---

## ۲. لیست نامرتب — `<ul>`

```html
<ul>
    <li>HTML</li>
    <li>CSS</li>
    <li>JavaScript</li>
</ul>
```

- **unordered** — ترتیب مهم نیست
- هر آیتم فقط در `<li>`
- bullet پیش‌فرض مرورگر (بعداً با CSS عوض می‌شود)

---

## ۳. لیست مرتب — `<ol>`

```html
<ol>
    <li>نصب VS Code</li>
    <li>ساخت index.html</li>
    <li>باز کردن در مرورگر</li>
</ol>
```

### ۳.۱. attributeهای ol

```html
<ol start="5">
    <li>مرحله پنج</li>
</ol>

<ol reversed>
    <li>اول (نمایش ۳)</li>
    <li>دوم (نمایش ۲)</li>
    <li>سوم (نمایش ۱)</li>
</ol>

<ol type="a">
    <li>بند الف</li>
    <li>بند ب</li>
</ol>
```

| type | نمایش |
|------|--------|
| `1` | ۱، ۲، ۳ (پیش‌فرض) |
| `a` | a، b، c |
| `A` | A، B، C |
| `i` | i، ii، iii |
| `I` | I، II، III |

---

## ۴. لیست تو در تو

```html
<ul>
    <li>Front-end
        <ul>
            <li>HTML</li>
            <li>CSS</li>
        </ul>
    </li>
    <li>Back-end
        <ul>
            <li>Node.js</li>
            <li>Python</li>
        </ul>
    </li>
</ul>
```

**قانون:** لیست داخل `<li>` والد — نه مستقیم داخل ul/ol.

---

## ۵. لیست تعریفی — `<dl>`

```html
<dl>
    <dt>HTML</dt>
    <dd>زبان نشانه‌گذاری صفحات وب</dd>

    <dt>CSS</dt>
    <dd>زبان استایل و چیدمان</dd>
</dl>
```

| تگ | نقش |
|----|-----|
| `dt` | اصطلاح (term) |
| `dd` | تعریف (description) |

یک `dt` می‌تواند چند `dd` داشته باشد.

---

## ۶. ul داخل ol و برعکس

```html
<ol>
    <li>خرید مواد
        <ul>
            <li>آرد</li>
            <li>شکر</li>
        </ul>
    </li>
    <li>پخت</li>
</ol>
```

---

## ۷. فایل‌های این جلسه

| فایل | موضوع |
|------|--------|
| [01_unordered_list.html](./examples/01_unordered_list.html) | ul |
| [02_ordered_list.html](./examples/02_ordered_list.html) | ol و attributeها |
| [03_nested_lists.html](./examples/03_nested_lists.html) | تو در تو |
| [04_description_list.html](./examples/04_description_list.html) | dl |

---

## ۸. اشتباهات رایج

| اشتباه | راه‌حل |
|--------|--------|
| متن مستقیم در ul | همه چیز در li |
| li خالی برای فاصله | CSS margin |
| `-` دستی به‌جای ul | از ul استفاده کنید |
| dl بدون dt/dd | ساختار جفت |
| nested بدون li والد | li دور لیست داخلی |

---

## ۹. دسترس‌پذیری

Screen reader تعداد آیتم و سطح nested را اعلام می‌کند. برای منوهای ناوبری بعداً از `<nav>` + ul استفاده می‌کنیم.

### ۹.۱. لیست و SEO

موتورهای جستجو ساختار لیست را برای فهم محتوا (مراحل، ویژگی‌ها) استفاده می‌کنند. لیست معنادار بهتر از خطوط `-` دستی است.

### ۹.۲. چند dd برای یک dt

```html
<dl>
    <dt>JavaScript</dt>
    <dd>زبان برنامه‌نویسی مرورگر</dd>
    <dd>همچنین با Node.js سمت سرور</dd>
</dl>
```

---

## ۱۰. مقایسه ul و ol

| ویژگی | ul | ol |
|-------|----|----|
| ترتیب | مهم نیست | مهم است |
| شمارنده | bullet | عدد/حرف |
| کاربرد | منو، ویژگی‌ها | دستورالعمل، رتبه‌بندی |
| nested | بله | بله |

---

## ۱۱. مثال کاربردی — دستور کار

```html
<h2>راه‌اندازی پروژه</h2>
<ol>
    <li>پوشه پروژه بسازید</li>
    <li>فایل <code>index.html</code> ایجاد کنید</li>
    <li>ساختار پایه را کپی کنید:
        <ul>
            <li>DOCTYPE</li>
            <li>head با charset</li>
            <li>body با h1</li>
        </ul>
    </li>
    <li>در مرورگر باز کنید</li>
</ol>
```

---

## ۱۲. لیست در پاراگراف؟

متن معمولی **خارج** li است؛ فقط محتوای آیتم داخل li:

```html
<!-- ✅ -->
<ul>
    <li>آیتم اول</li>
</ul>

<!-- ❌ -->
<ul>
    <p>آیتم</p>
</ul>
```

---

## ۱۳. type در HTML5

`type` روی ol ظاهر شمارنده را عوض می‌کند؛ **معنای** ترتیب از ol می‌آید نه از type. برای دسترس‌پذیری ol را حفظ کنید.

---

## ۱۴. اشتباهات بیشتر

### ❌ br بین li برای فاصله

```html
<li>اول</li>
<br>
<li>دوم</li>
```

از CSS `margin` استفاده کنید (جلسه CSS).

### ❌ li بدون ul/ol والد

```html
<li>تنها</li>  <!-- نامعتبر -->
```

---

## ۱۵. بهترین شیوه‌ها

1. **لیست واقعی** — نه dash تایپ‌شده
2. **عنوان قبل لیست** — h2/h3 توضیح دهد لیست چیست
3. **nested عمیق کم** — بیش از ۳ سطح گیج‌کننده است
4. **dl برای واژه** — نه جدول دو ستونی fake
5. **ol برای مراحل** — ul برای مجموعه

---

## ۱۶. تمرین کلاسی

1. ul مهارت‌ها
2. ol مراحل پخت یا نصب
3. nested منوی دوره
4. dl واژه‌نامه ۵ اصطلاح

**تکلیف:** [question.md](./exercises/question.md)

---

## ۱۱. خلاصه

- ✅ ul — بدون ترتیب؛ ol — با ترتیب
- ✅ li تنها فرزن مستقیم مجاز (با متن و لیست)
- ✅ dl برای term/definition
- ✅ nested با li والد

**بعد:** [۶ — لینک‌ها](../session-06/)

---

**قبل:** [۴ — قالب‌بندی](../session-04/) | **بعد:** [۶ — لینک‌ها](../session-06/)

---

## راهنمای یادگیری خودآموز

### عنوان، شماره و زمان

**جلسهٔ 5 — فهرست‌ها | ۹۰ دقیقه**

### پیش‌نیازها

- مفاهیم جلسهٔ 4: معنا و قالب‌بندی متن
- توانایی باز کردن فایل HTML در مرورگر و استفادهٔ پایه از DevTools
- اجرای مثال‌های جلسه پیش از شروع تمرین

### اهداف قابل‌اندازه‌گیری

در پایان جلسه باید بتوانید:

1. مفهوم «فهرست‌ها» را با یک مثال و واژگان فارسی/English توضیح دهید.
2. نمونهٔ موجود را بدون کپی کورکورانه بازسازی و نتیجه را در مرورگر بررسی کنید.
3. حداقل سه خطای رایج این موضوع را با DevTools تشخیص دهید.
4. راه‌حل خود را از نظر معنا، خوانایی، responsive بودن و accessibility ارزیابی کنید.

### چرا این جلسه مهم است؟

موضوع **فهرست‌ها** یک جزیرهٔ جدا نیست؛ مستقیماً روی ساختار و معنای سند در DOM اثر می‌گذارد. یادگیری آن به‌صورت تصمیم‌محور باعث می‌شود به‌جای حفظ کردن تگ یا property، بتوانید برای محتوای واقعی راه‌حل درست انتخاب کنید و دلیل انتخابتان را توضیح دهید.

### مفهوم عمیق: از کد تا نتیجه

مرورگر کد را از بالا به پایین parse می‌کند، آن را به ساختارهای داخلی مانند DOM و CSSOM تبدیل می‌کند و سپس style، layout و paint را انجام می‌دهد. هنگام کار با **فهرست‌ها** همیشه سه پرسش داشته باشید: «معنای این کد چیست؟»، «مرورگر چگونه آن را محاسبه می‌کند؟» و «کاربر با keyboard، موبایل یا صفحه‌خوان چه تجربه‌ای دارد؟». این مدل ذهنی از آزمون‌وخطای تصادفی جلوگیری می‌کند.


### سه اشتباه رایج و اصلاح آن‌ها

1. **کپی‌کردن بدون پیش‌بینی نتیجه:** پیش از refresh بنویسید انتظار دارید چه تغییری ببینید؛ سپس با DevTools اختلاف را پیدا کنید.
2. **حل ظاهر و نادیده‌گرفتن معنا:** ابتدا ساختار، ترتیب محتوا و دسترسی با keyboard را درست کنید؛ بعد سراغ polish بصری بروید.
3. **تغییرهای بزرگ و تست دیرهنگام:** هر بار یک تغییر کوچک انجام دهید و در عرض باریک و عریض بررسی کنید.
4. **استفاده از workaround فوری:** پیش از `!important`، selector پیچیده یا مقدار جادویی، علت اصلی را در Computed/Box Model بیابید.

### مثال گام‌به‌گام

فایل [`examples/01_unordered_list.html`](./examples/01_unordered_list.html) را مبنا قرار دهید:

1. فایل را در مرورگر باز کنید و بدون دیدن کد، اجزای صفحه را نام ببرید.
2. سورس را بخوانید و هر بخش مرتبط با «فهرست‌ها» را با یک کامنت مشخص کنید.
3. یک مقدار یا عنصر را تغییر دهید و نتیجهٔ مورد انتظار را قبل از refresh یادداشت کنید.
4. با Inspect، مقدار نهایی و دلیل اعمال‌شدن آن را بررسی کنید.
5. تغییر را با keyboard و عرض ۳۲۰ پیکسل آزمایش کنید.
6. کد آزمایشی را به نسخه‌ای خوانا با نام‌گذاری معنادار refactor کنید.

### الگوی بهتر (Better Pattern)

چرخهٔ **content → structure → style → test → refactor** را رعایت کنید: با محتوای واقعی شروع کنید، کمترین ساختار معنایی لازم را بسازید، style را از ساده به پیچیده اضافه کنید، در هر مرحله تست کنید و فقط پس از کارکرد صحیح abstraction بسازید. این الگو از over-engineering و CSS شکننده جلوگیری می‌کند.

### تمرین‌ها

#### تمرین آسان

مثال اصلی را از حافظه در یک فایل تازه بازسازی کنید و سه تصمیم خود دربارهٔ «فهرست‌ها» را در کامنت‌ها توضیح دهید. سپس یک screenshot یا یادداشت از بررسی DevTools ثبت کنید.

#### تمرین چالشی

مثال را با محتوای فارسی واقعی گسترش دهید؛ نسخه باید در عرض ۳۲۰ تا ۱۲۸۰ پیکسل، با Tab، و بدون خطای مسیر فایل کار کند. یک محدودیت عمدی ایجاد کنید، آن را تشخیص دهید و علت/اصلاح را بنویسید.

### راهنمای حل

- ابتدا معیار پذیرش را بنویسید: محتوای کامل، ساختار معتبر، نبود overflow و focus قابل‌مشاهده.
- نسخهٔ آسان را با کوچک‌ترین کد ممکن بسازید و با نمونه مقایسه کنید.
- در تمرین چالشی از Responsive Mode و پنل Computed استفاده کنید؛ فقط پس از یافتن علت، کد را تغییر دهید.
- پاسخ یکتا نیست؛ راه‌حل خوب باید **قابل توضیح، قابل تست و قابل نگهداری** باشد.

### خودآزمایی

1. موضوع اصلی این جلسه در کدام مرحله از کار مرورگر یا تجربهٔ کاربر اثر می‌گذارد؟
2. چگونه بدون حدس‌زدن می‌فهمید کد شما واقعاً اعمال شده است؟
3. یک تصمیم ضعیف در این موضوع چه اثری بر موبایل یا accessibility دارد؟
4. چه زمانی راه‌حل ساده‌تر از abstraction پیچیده بهتر است؟

<details>
<summary>راهنمای پاسخ</summary>

پاسخ باید به‌ترتیب به مدل DOM/CSSOM و render، مشاهده در DevTools و آزمون واقعی، اثر بر ترتیب/خوانایی/focus، و اصل «کمترین پیچیدگی لازم» اشاره کند. پاسخ را با یک مثال از فایل همین جلسه کامل کنید.
</details>

### جمع‌بندی و پل جلسهٔ بعد

اکنون باید بتوانید **فهرست‌ها** را توضیح دهید، در نمونه تشخیص دهید، به‌صورت مرحله‌ای پیاده کنید و با ابزار مرورگر بسنجید. گام بعدی **جلسهٔ 6: لینک‌ها و مسیرها** است؛ پیش از رفتن، تمرین چالشی را نگه دارید تا در جلسهٔ بعد روی همان خروجی توسعه دهید.

