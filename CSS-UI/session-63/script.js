console.log("session-63 ready — Bootstrap block starts");
document.getElementById("btn")?.addEventListener("click", () => {
  const out = document.getElementById("output");
  if (out) {
    out.textContent =
      "مسیر Bootstrap (۶۳–۷۸):\n" +
      "Grid → Utilities → Components → Sass customize → پروژه‌ها\n" +
      "Tailwind از ۷۹ جداگانه شروع می‌شود.";
  }
});
