# جلسه 19: Display (2 ساعت)

## اهداف
- `block`، `inline`، `inline-block`، `none`
- تفاوت جریان سند

```css
.ib { display: inline-block; }
.hidden { display: none; }
```

`visibility: hidden` فضا را نگه می‌دارد؛ `display: none` از جریان حذف می‌کند.

## گام بعد
[جلسه 20](../session-20/index.html)
