# چک‌لیست capstone

1. [ ] `docker compose up -d --build` بدون خطا
2. [ ] `http://localhost:8080/` صفحه می‌دهد
3. [ ] دو بار `http://localhost:8080/api/` — `hits` زیاد می‌شود
4. [ ] `compose logs` خطای واضح ندارد
5. [ ] `compose down` (بدون `-v` مگر عمدی)
6. [ ] اپ‌های Clinic دست نخورده‌اند
