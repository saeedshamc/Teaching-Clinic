// ============================================================
// تمرین جلسه 7 — پاسخ نمونه
// ============================================================
int[] data = { 4, 9, 2, 7 };
int max = data[0];
foreach (var x in data) if (x > max) max = x;
Console.WriteLine(max);
