# ============================================================
# جلسه ۱ — ساختار فایل و نحوه اجرا
# فایل: 04_run_and_structure.py
# ============================================================
# این فایل نشان می‌دهد یک اسکریپت Python چگونه ساختار دارد
# و چطور از ترمینال اجرا می‌شود:
#   python examples/04_run_and_structure.py

# --- بخش ۱: shebang (اختیاری در ویندوز، مفید در Linux/Mac) ---
# #!/usr/bin/env python3

# --- بخش ۲: docstring ماژول (توضیح کلی فایل) ---
"""نمایش ساختار ساده یک اسکript Python قابل اجرا."""

# --- بخش ۳: importها (در این جلسه خالی است) ---

# --- بخش ۴: کد اصلی ---
def main():
    """تابع اصلی — الگوی رایج برای اسکریپت‌های بزرگ‌تر."""
    print("=" * 40)
    print("  اجرای موفق اسکریپت Python")
    print("=" * 40)
    print()
    print("مراحل اجرا:")
    print("  1) ترمinal را باز کنید")
    print("  2) به پوشه session-01-intro بروید")
    print("  3) دستور: python examples/04_run_and_structure.py")
    print()
    print("نسخه Python:", end=" ")
    import sys
    print(sys.version.split()[0])


# --- بخش ۵: نقطه ورود ---
# فقط وقتی فایل مستقیم اجرا شود (نه import)، main صدا زده می‌شود
if __name__ == "__main__":
    main()
