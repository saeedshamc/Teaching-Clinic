# تمرین‌های جلسه ۱۹ — Type Hints

## کلاسی (۳۰ دقیقه)

### ۱. annotate جمع
`add(a: int, b: int) -> int`

### ۲. list و dict
تابع `total_length(words: list[str]) -> int`

### ۳. Optional
`find(items: list[int], target: int) -> int | None`

---

## خانه (۴۵ دقیقه)

### ۴. TypedDict
`Student` با name و score — تابع `is_passed(student) -> bool`

### ۵. Callable
`apply_all(numbers, func: Callable[[int], int]) -> list[int]`

---

پاسخ نمونه: [exercise_01.py](./exercise_01.py) | [exercise_02.py](./exercise_02.py)
