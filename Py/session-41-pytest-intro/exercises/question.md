# تمرین‌های جلسه ۴۱ — pytest

## کلاسی (۳۰ دقیقه)

### ۱. test_add / test_subtract
برای توابع `add` و `subtract` حداقل ۲ assert هر کدام.

### ۲. parametrize
`test_abs` با ورودی‌های مثبت، منفی و صفر.

### ۳. pytest.raises
برای تابعی که ورودی خالی را reject می‌کند.

---

## خانه (۴۵ دقیقه)

### ۴. fixture cart
fixture سبد خرید + تست `total()`.

### ۵. فایل tests/
ساختار `tests/test_math.py` با ۵ تست — اجرا با `pytest -v`.

---

پاسخ: [exercise_01.py](./exercise_01.py) | [exercise_02.py](./exercise_02.py)
