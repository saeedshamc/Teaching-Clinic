package main

import (
	"errors"
	"fmt"
)

func divide(a, b float64) (float64, error) {
	if b == 0 {
		return 0, errors.New("?????????? ??? ???")
	}
	return a / b, nil
}
func main() {
	defer fmt.Println("????? ??????")
	result, err := divide(10, 4)
	if err != nil {
		fmt.Println("???:", err)
		return
	}
	fmt.Println("?????:", result)
}
